namespace TheSoftwareGorilla.TDD.Money.Tests;

#region TO DO's
#endregion

[TestFixture]
[Description("Subtract Tests")]
public class SubtractTest
{
    [Test]
    public void TestMinusReturnsSubtract() {
        // Money usd = Money.For(5, "USD");
        // Money zar = Money.For(17, "ZAR");
        // Money result = usd.Minus(zar);
        // Assert.IsInstanceOf<Subtract>(result);
        // Subtract subtract = (Subtract) result;
        // Assert.That(subtract.Minuend, Is.EqualTo(usd));
        // Assert.That(subtract.Subtrahend, Is.EqualTo(zar));
    }
    
}
